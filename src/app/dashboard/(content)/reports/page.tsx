import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Reports",
  description: "Reports page for iSpeak",
};

export default async function ReportsPage() {
  return <div>Reports</div>;
}
