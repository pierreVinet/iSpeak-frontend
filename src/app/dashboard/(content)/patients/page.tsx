import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Patients",
  description: "Patients page for iSpeak",
};

export default async function PatientsPage() {
  return <div>Patients</div>;
}
